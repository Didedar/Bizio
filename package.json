{
    "name": "bizio",
    "private": true,
    "engines": {
        "node": "18"
    },
    "dependencies": {
        "serve": "^14.2.0"
    },
    "scripts": {
        "build": "cd frontend && npm install && npm run build",
        "start": "serve -s frontend/dist -l ${PORT:-3000}"
    }
}